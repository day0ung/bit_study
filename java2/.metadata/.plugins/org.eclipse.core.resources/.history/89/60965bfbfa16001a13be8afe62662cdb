package awtSample01;

import java.awt.Frame;

public class WindowTest extends Frame {
	public WindowTest() {
		super("간단한 예제");
		//frame에서 상속받은메소드
		setSize(640, 480); // 윈도우의 크기(폭, 높이)
		setLocation(0, 0); //윈도우가 실행 위치(x오른쪽으로갈수록 늘어남, y 밑으로내려갈수록 늘어남)
		setVisible(true); // 윈도우 시각화
	}
}
